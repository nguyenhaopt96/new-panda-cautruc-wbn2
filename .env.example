# No environment variables or external API keys are required.
# Footage stays in the browser (IndexedDB). Rendering uses the bundled server-side FFmpeg binary.
# PORT is supplied automatically by the host; local development falls back to port 3000.
